{
  "name": "sub",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ NftBazaar/sub",
    "create-local": "graph create --node http://localhost:8020/ NftBazaar/sub",
    "remove-local": "graph remove --node http://localhost:8020/ NftBazaar/sub",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 NftBazaar/sub --network localhost",
    "test": "graph test"
  },
  "dependencies": {},
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
